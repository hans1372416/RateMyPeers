<%= form_with(model: evaluate, local: true) do |form| %>
  <% if evaluate.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(evaluate.errors.count, "error") %> prohibited this evaluate from being saved:</h2>

      <ul>
      <% evaluate.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :attempt %>
    <%= form.check_box :attempt %>
  </div>

  <div class="field">
    <%= form.label :user_id %>
    <%= form.number_field :user_id %>
  </div>

  <div class="field">
    <%= form.label :ratee_id %>
    <%= form.number_field :ratee_id %>
  </div>

  <div class="field">
    <%= form.label :group_id %>
    <%= form.number_field :group_id %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
