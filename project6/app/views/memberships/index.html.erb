<h1>Students</h1>

<table>
  <thead>
    <tr>
      <th>User</th>
      <th>Actions</th>
    </tr>
  </thead>

  <tbody>
    <% @memberships.each do |m| %>
        <% if Group.find(m.group_id).id %>
      	   <tr>
              <td><%= User.find(m.user_id).email %></td>
              <td><%= link_to 'Remove Member', m, method: :delete, data: { confirm: 'Are you sure?' }, :class=> "btn btn-danger" %><br></td>
           </tr>
        <% end %>
    <% end %>
  </tbody>
</table>

<br><%= link_to 'Back to Groups', groups_path, :class => "btn btn-default" %>
<br><%= link_to 'Back to Classes', courses_path, :class => "btn btn-default" %>
