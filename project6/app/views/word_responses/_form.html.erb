<%= form_with(model: word_response, local: true) do |form| %>
  <% if word_response.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(word_response.errors.count, "error") %> prohibited this word_response from being saved:</h2>

      <ul>
      <% word_response.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
