<!DOCTYPE html>
<html>
  <head>
    <title>Project 6</title>
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>
    <link href="https://fonts.googleapis.com/css?family=Montserrat:100,100i,200,200i,300,300i,400,400i,500,500i,600,600i,700,700i,800,800i,900,900i|Roboto:100,100i,300,300i,400,400i,500,500i,700,700i,900,900i" rel="stylesheet">
    <%= stylesheet_link_tag  "style.css" %>
    <%= javascript_include_tag 'application', 'data-turbolinks-track': 'reload' %>
  </head>

  <body>

      <% if user_signed_in? %>
        	<% if current_user.try(:admin?) %>
			<%= render "layouts/nav-instructor" %>
      		<% else %>
      			<%= render "layouts/nav-user" %>
      		<% end %>
      <% else %>
      	<%= render "layouts/nav-anon" %>
      <% end %>
    
    
    <div class="content">
        <% flash.each do |message_type, message| %>
        <div class="divforalerts"> <%= message %> </div>
        <% end %>
      	<%= yield %>
    </div>

  </body>
</html>
