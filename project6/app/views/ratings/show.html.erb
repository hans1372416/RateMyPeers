Evaluation Submitted<br>
Group: <%= Group.find(@rating.group_id).gname %><br>
Rated: <%= User.find(Evaluate.find(@rating.id).ratee_id).Fname %> <%= User.find(Evaluate.find(@rating.id).ratee_id).Lname %><br>
Score: <%= @rating.score %><br>
Comments: <%= @rating.comments %><br>
<% Evaluate.find(@rating.id).update_attribute :attempt, true %>

<%= link_to 'Back', groups_path %>
